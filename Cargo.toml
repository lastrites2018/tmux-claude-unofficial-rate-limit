[package]
name = "rate-limit"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "3"
base64 = "0.22"
tempfile = "3"
zeroize = "1"

[profile.release]
opt-level = "s"
strip = true
lto = true
